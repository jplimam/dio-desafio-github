/*const DIO = 'Digital Innovation One'

let firstName = 'João';
let lastName = 'Luiz';

let fullName = `Nome completo: ${firstName} ${lastName}`;

console.log(fullName);

let nome = 'João';
let sobrenome = 'Pedro';

nome.concat(sobrenome); //concat concatena duas strings
let concatenado = nome.concat(sobrenome);

concatenado.length; // length mostra o tamanho da string

let exemplo = new String("blablabla"); // com letra maiúscula string vira objeto

let vazio = "";

nome "João"
nome [1] "o"
nome.length 4

// espaço em concatenação
concatenado = nome + " " + sobrenome; || concatenado = `${nome} ${sobrenome}`; 
// JoãoPedro para João Pedro

concatenado = nome + "\n " + sobrenome; || concatenado = `${nome}

${sobrenome}`
// JoãoPedro para 
João 
Pedro

// imprimir aspas
concatenado = "\""

// Separar palavras de frase
let frase = "Olá, tudo bem?"
frase.split(" ")
["Olá,", "tudo", "bem?"]

// limpar console
console.clear()

// procurar palavras na frase
frase.includes("tudo")
retorna valor booleano, true se tiver na frase e false se não

// procurar inicial da frase
frase.startsWith("O")
retorna valor booleano, true se iniciar a frase e false se não

// procurar final da frase
frase.endsWith("?")
retorna valor booleano, true se iniciar a frase e false se não


// Altera dado na frase
frase.replace(",", "!")
Passará de "Olá, tudo bem?" para "Olá! tudo bem?"
// Porém após modificar a frase deveremos declarar como outra string
let stringModificada = frase.replace(",", "!")
// então
frase
"Olá, tudo bem?"
stringModificada
"Olá! tudo bem?"

*/